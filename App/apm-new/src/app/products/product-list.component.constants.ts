import { ShowImageTextConstants } from './product-list.component.types';

const SHOW_IMAGE_TEXT: ShowImageTextConstants = {
  visible: "Show image",
  hidden: "Hide image"
};

export { SHOW_IMAGE_TEXT };

